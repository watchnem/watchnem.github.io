---
layout: null
permalink: /search.json
---
[
{% assign posts = site.posts | where_exp: "p", "p.published != false" %}
{% assign first = true %}
{% for p in posts %}
  {% unless p.categories contains "히든" %}
    {% unless first %},{% endunless %}
    {
      "title": {{ p.title | jsonify }},
      "url": {{ p.url | absolute_url | jsonify }},
      "date": "{{ p.date | date: "%Y-%m-%d" }}",
      "categories": {{ p.categories | jsonify }},
      "tags": {{ p.tags | jsonify }},
      "excerpt": {{ p.content | strip_html | normalize_whitespace | truncatewords: 40 | jsonify }}
    }
    {% assign first = false %}
  {% endunless %}
{% endfor %}
]
